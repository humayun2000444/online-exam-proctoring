// config.js

const config = {
    API_BASE_URL: 'http://localhost:5000', // Change this later for production easily
    // You can add more common config here later, like APP_NAME, VERSION, etc.
};

export default config;
